{
  "name": "@securelore/review-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "smoke": "npm run build && node dist/scripts/smoke-review.js",
    "eval:regression": "npm run build && node dist/scripts/regression-benchmark.js"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
